# Debug Verification Report - Select Prompt Screen

## ✅ Widget Chain Verification

### Confirmed Correct Files Being Used:

1. **Entry Point**: `lib/ui/bottom_sheets/add_mono_bottom_sheet.dart`
   - Method: `_buildPromptSelector()` (line 631)
   - Uses: `PromptCarousel` widget

2. **Carousel Widget**: `lib/ui/create/widgets/prompt_carousel.dart`
   - Imports: `../../widgets/one_short_prompt_card.dart` (line 3)
   - Uses: `OneShortPromptCard` widget (line 130)

3. **Card Widget**: `lib/ui/widgets/one_short_prompt_card.dart`
   - ✅ **THIS IS THE CORRECT FILE TO EDIT**
   - Displays: prompt title, context, duration, and category tag

### ⚠️ Unused/Different File Found:

- `lib/create_mono/widgets/prompt_card.dart` - **NOT USED** by Select Prompt screen
  - This is a different widget used elsewhere in the app
  - Do NOT edit this file for Select Prompt changes

---

## 🔍 Test Changes Applied

### 1. Prompt Title Change
**File**: `lib/data/prompt_repository.dart` (line 101)
- **Changed**: `'${category.toUpperCase()} IDEA #$i'`
- **To**: `'${category.toUpperCase()} TEST #$i (DEBUG)'`
- **Expected Result**: All prompt titles should now show "COMEDY TEST #1 (DEBUG)" instead of "COMEDY IDEA #1"

### 2. Category Tag Visual Test
**File**: `lib/ui/widgets/one_short_prompt_card.dart` (lines 79-96)
- **Changed**: Category tag styling to be extremely obvious:
  - Background: `Colors.black` (was pink #FFB3C1)
  - Text: `Colors.white` (was deep rose #7A003C)
  - Border: `Colors.red` with `width: 2` (was no border)
- **Expected Result**: Category tags should have black background, white text, and red border

---

## 🧹 Clean & Rebuild Commands

Run these commands in order:

```bash
flutter clean
flutter pub get
flutter run
```

Or if using an IDE:
1. Run `flutter clean`
2. Run `flutter pub get`
3. Full restart the app (not hot reload)

---

## ✅ Verification Checklist

After rebuilding, check:

- [ ] Prompt titles show "COMEDY TEST #1 (DEBUG)" (or similar for other categories)
- [ ] Category tags have **black background**
- [ ] Category tags have **white text**
- [ ] Category tags have **red border** (2px width)

If you see these changes, the build is using the correct files! ✅

If you DON'T see these changes:
- The app might be running from a different directory
- There might be cached build files
- Try `flutter clean` again and full restart

---

## 📝 Next Steps

Once you confirm the test changes are visible:

1. **Revert the test changes**:
   - Change title back to `'${category.toUpperCase()} IDEA #$i'`
   - Change category tag back to pink (#FFB3C1) background and deep rose (#7A003C) text
   - Remove the red border

2. **Apply your actual design changes** to `lib/ui/widgets/one_short_prompt_card.dart`

3. **Rebuild** the app to see your final design

---

## File Path Summary

**Correct files to edit for Select Prompt screen:**
- ✅ `lib/ui/bottom_sheets/add_mono_bottom_sheet.dart`
- ✅ `lib/ui/create/widgets/prompt_carousel.dart`
- ✅ `lib/ui/widgets/one_short_prompt_card.dart` ← **Main card widget**
- ✅ `lib/data/prompt_repository.dart` ← **Prompt data source**

**Do NOT edit (different widget):**
- ❌ `lib/create_mono/widgets/prompt_card.dart` (unused for this screen)

